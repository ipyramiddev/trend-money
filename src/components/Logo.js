import logo from "./../logo.png";

function Logo(props) {
    return (
        <div className=" w-auto">
            <img height={45} width={150} src={logo} />
        </div>
    );
}

export default Logo;
