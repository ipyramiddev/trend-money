// Create a component for a user to mint a wagmeme, and then add it to the explorer.
import { wagMint } from '../hooks/useWag';
const WagMemeContainer = () => {
    return (
        <div>
            {/* <WagMeme /> */}
            </div>
    )
}
export default WagMemeContainer;